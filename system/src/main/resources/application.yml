spring:
  thymeleaf:
    mode: LEGACYHTML5
    cache: false

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/BlogSystem?useUnicode=true&characterEncoding=utf-8&serverTimezone=CTT
    username: root
    password: 123123

  jpa:
    hibernate:
      ddl-auto: update
logging:
  level:
    root: info
    com.my: debug
  file: log/Blog.log
